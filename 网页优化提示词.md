# 新闻树交互式新闻聚合网站开发任务

## 1. 项目背景

这是我目前正在开发的新闻聚合项目：

https://github.com/Learner-ning/news-nb

参考项目：

https://github.com/ourongxing/newsnow

请基于现有项目进行功能扩展和 UI 重构。

不要重新创建一个独立项目。

不要破坏当前项目已有的新闻获取、分类、数据处理、新闻跳转等功能。

本次开发的核心目标是：

将现有新闻聚合网站改造成一个真正的“交互式新闻树”。

---

# 2. 最重要的视觉要求

## 绝对禁止把新闻树做成粒子网络

最终页面不能出现下面这种效果：

- 大量散落的发光点
- 一堆独立圆点
- 节点之间用线连接
- 类似知识图谱
- 类似星空粒子系统
- 类似网络拓扑图
- 一个中心点向四周发散
- 一堆悬浮卡片漂浮在页面中

我要的是：

**一棵真正的树。**

必须能够在视觉上明确看到：

树干

↓

主枝

↓

分枝

↓

新闻节点 / 新闻卡片

整个页面首先看起来应该是一棵树，然后用户进一步观察之后才能发现这棵树实际上由新闻数据组成。

---

# 3. 新闻树的基本结构

使用二维树结构。

推荐使用：

**SVG**

如果经过分析发现 Canvas 更适合当前项目，也可以使用 Canvas 2D。

不要默认使用 Three.js。

除非当前项目实际情况明确需要，否则不要引入 3D。

树结构必须具备真实的层级。

示意：

```text
                         新闻树
                           │
                       ────┼────
                      /    │    \
                   主枝    主枝    主枝
                  /  \     │     /  \
                分枝 分枝  分枝  分枝 分枝
               / \    \    │    /  \
             新闻 新闻 新闻 新闻 新闻 新闻
```

树干必须有明显的宽度。

主枝必须从树干自然生长出来。

分枝必须连接到主枝。

新闻必须真实挂载到分枝末端或者树叶位置。

---

# 4. 树的生成方式

不要手工绘制固定的一棵树。

树必须根据当前新闻数据动态生成。

数据结构大致应该形成：

```text
新闻
  ↓
新闻分类
  ↓
分类主枝
  ↓
分类分枝
  ↓
具体新闻
```

例如：

```text
                    ┌── AI 新闻
                    │     ├── 新闻 1
                    │     ├── 新闻 2
                    │     └── 新闻 3
                    │
              ──────┤
                    │
                    ├── 科技新闻
                    │     ├── 新闻 4
                    │     ├── 新闻 5
                    │     └── 新闻 6
                    │
                    ├── 财经新闻
                    │     ├── 新闻 7
                    │     └── 新闻 8
                    │
                    └── 体育新闻
                          ├── 新闻 9
                          └── 新闻 10
```

具体分类必须使用项目当前真实存在的新闻分类。

不要为了视觉效果创建虚假的分类。

---

# 5. 新闻如何出现在树上

这是整个项目最关键的设计。

新闻不能只是一个圆点。

新闻应该成为：

**树叶、叶片标签或者挂在枝条末端的新闻卡片。**

推荐设计：

每一个新闻在树枝末端形成一个小型新闻叶片。

例如：

```text
            /
-----------/------[ 新闻标题 ]
          /
         /
        /
```

或者：

```text
                  🍃 新闻标题
                 /
        --------/
```

新闻标题应该直接与树枝产生视觉联系。

用户看到新闻标题时，可以明显看出来这个新闻属于哪一条枝。

---

# 6. 新闻节点具体设计

每条新闻可以设计成一个轻量化叶片。

叶片中显示：

新闻标题

必要时显示：

来源
发布时间

例如：

```text
        ╭────────────────╮
───────▶  OpenAI 发布新模型
        │ 科技 · 10分钟前  │
        ╰────────────────╯
```

或者采用更加自然的叶片形状：

```text
        ╭──────────────╮
───────╯ AI 模型更新    ╰────
```

具体视觉形式可以根据整体设计调整。

但是必须满足：

新闻是“挂在树上”的。

不能让新闻漂浮在树周围。

---

# 7. 树的布局算法

必须设计一个真正的树布局算法。

不要随机生成节点。

不要使用：

```javascript
Math.random()
```

随机把新闻散落在画布上。

必须保证：

每次页面加载树的结构稳定。

同一批新闻得到基本稳定的布局。

新闻分类对应固定主枝。

新闻根据分类排列到对应分枝。

新闻数量变化时，可以重新计算分枝。

树枝之间不能大量重叠。

新闻标题之间不能严重重叠。

---

# 8. 推荐的树布局逻辑

树干固定在屏幕中间下方。

例如：

```text
                    新闻
          ┌──────────┼──────────┐
         /           │           \
        /            │            \
       /             │             \
      /              │              \
   分类 A          分类 B          分类 C
   /  \            /  \            / \
 新闻 新闻        新闻 新闻       新闻 新闻

                 │
                 │
                 │
                 │
                树干
                 │
                 │
                 │
                树根
```

树干位置：

```text
x = viewport.width / 2
```

树干从页面底部向上生长。

主枝从树干向左右两侧展开。

主枝数量由新闻分类决定。

每个分类至少拥有一条主枝。

新闻数量多时，在主枝上继续生成二级分枝。

---

# 9. 树枝形态

不要用大量直线。

树枝应该具有自然曲线。

推荐 SVG：

```svg
<path
  d="M500 700 C450 600 420 520 300 420"
  fill="none"
/>
```

通过 Bezier 曲线模拟自然树枝。

树干应该粗。

主枝应该比树干细。

分枝比主枝更细。

例如：

```text
树干：12px

主枝：7px

二级枝：4px

三级枝：2px
```

具体数值根据页面比例调整。

---

# 10. 树干设计

树干不能只是：

```text
一条竖线
```

需要具有明显的树干形态。

可以使用：

SVG Path

多层路径

渐变宽度

自然弯曲

使树干看起来像真实的抽象数字树。

例如：

```text
                  /────── 新闻
             ────/
          ───
       ──
      │
      │
     ╱
    │
   ╱
  │
 │
```

树干底部较粗。

向上逐渐变细。

---

# 11. 新闻数据与树结构的绑定

建立一个专门的数据转换层。

例如：

```javascript
news
    ↓
transformNewsToTree()
    ↓
treeData
```

treeData 可以类似：

```javascript
{
  trunk: {},
  branches: [
    {
      id: "technology",
      name: "科技",
      children: [
        {
          id: "news-1",
          type: "news",
          title: "...",
          source: "...",
          publishedAt: "...",
          url: "..."
        }
      ]
    }
  ]
}
```

具体结构根据当前项目实际情况设计。

不要强行使用这个示例结构。

---

# 12. 新闻分类

页面底部增加分类导航。

例如：

```text
全部    AI    科技    财经    国际    国内    体育    娱乐
```

具体分类必须读取当前项目真实数据。

用户选择：

全部

则显示完整新闻树。

用户选择：

科技

则只显示科技新闻。

此时树应该重新计算布局。

不应该简单隐藏其他节点。

建议：

树枝进行重新布局。

不相关分类从树中移除。

当前分类成为主要视觉主体。

---

# 13. 新闻树交互

树需要支持：

### 鼠标 Hover

当鼠标移动到新闻叶片或者新闻节点上：

该新闻突出显示。

对应枝条轻微高亮。

显示新闻详情卡片。

例如：

```text
┌────────────────────────────┐
│ OpenAI 发布新模型           │
│                            │
│ 科技                       │
│ OpenAI                     │
│ 10分钟前                   │
│                            │
│ 点击查看新闻               │
└────────────────────────────┘
```

不要让卡片占据屏幕中央。

尽量出现在新闻节点附近。

---

# 14. 点击新闻

点击新闻之后：

使用当前项目已有的新闻跳转逻辑。

不要重新建立新的新闻链接系统。

如果当前项目存在：

新闻详情页面

则进入详情页面。

如果当前项目使用：

原始新闻 URL

则继续使用原始 URL。

保证所有已有新闻都可以正常访问。

---

# 15. 树的拖拽

允许用户拖拽整个新闻树。

例如：

```text
鼠标按下
    ↓
拖动
    ↓
整棵树移动
```

拖拽不会改变树的内部结构。

只改变整个 SVG / Canvas 的 viewport transform。

---

# 16. 缩放

支持：

鼠标滚轮缩放

缩放中心应该基于鼠标位置。

不要让用户缩放之后树突然跑到屏幕角落。

需要实现：

```text
zoom in
zoom out
reset
```

可以在页面右下角提供：

+ 
-

重新定位

按钮。

---

# 17. 树的生长动画

进入网站时：

不是所有树枝瞬间出现。

而是：

树干

↓

主枝

↓

分枝

↓

新闻叶片

依次出现。

例如：

```text
0ms

树干出现

↓

300ms

主枝出现

↓

600ms

分枝出现

↓

900ms

新闻叶片出现
```

动画时间不宜过长。

推荐：

300ms
500ms

等轻量动画。

不要做夸张动画。

---

# 18. 禁止使用粒子系统代替树

非常重要。

不要使用：

```javascript
particles
```

来生成新闻树。

不要用：

```text
随机圆点 + 随机连线
```

模拟树。

不要让页面最终看起来像：

```text
         ·     ·
    ·          ·       ·
       ·   ·
  ·        ·     ·
       ·
```

这种效果完全不是我要的。

必须出现：

真实的树干

真实的主枝

真实的分枝

真实的新闻叶片

---

# 19. 背景

背景可以使用深色沉浸式风格。

默认：

夜晚

低饱和深色背景

轻微星空

极少量环境粒子

但是：

星空是背景。

粒子只是环境。

新闻树本身不能由粒子组成。

新闻树必须清晰可见。

---

# 20. 左侧控制栏

左侧提供显示方式切换。

至少：

```text
显示方式

● 新闻树

○ 新闻列表
```

未来预留：

时间线

但本次可以不实现。

---

# 21. 列表模式

切换到列表模式后：

隐藏新闻树。

显示普通新闻列表。

例如：

```text
科技

OpenAI 发布新模型                     10分钟前
苹果发布新产品                         20分钟前
英伟达推出新芯片                       30分钟前
```

列表模式需要直接使用当前项目真实新闻数据。

不要重新制造 Mock 新闻。

点击新闻之后：

使用和新闻树完全相同的跳转逻辑。

---

# 22. 顶部区域

顶部可以放：

项目名称

当前新闻分类

新闻更新时间

刷新按钮

但是顶部必须非常简洁。

不要做复杂 Header。

整个视觉重点始终是中央新闻树。

---

# 23. 底部分类栏

底部固定一个轻量导航。

例如：

```text
全部   AI   科技   财经   国际   国内   体育
```

分类栏不能遮住树。

需要考虑：

安全区域

移动端底部区域

---

# 24. 页面整体结构

最终推荐布局：

```text
┌─────────────────────────────────────────────┐
│                  News Tree                  │
│                              更新时间      │
│                                             │
│  ┌───────┐                                  │
│  │树模式 │             🌳                   │
│  │列表模式│          🌿 │ 🌿                 │
│  │       │       新闻 ─┤─ 新闻              │
│  └───────┘          🌿 │ 🌿                 │
│                         │                   │
│                         │                   │
│                       🌳🌳                  │
│                                             │
│                                             │
│        全部  AI  科技  财经  国际  体育      │
└─────────────────────────────────────────────┘
```

这里的 🌳 只是结构示意。

实际页面不要使用 Emoji 作为树。

---

# 25. 推荐技术方案

优先：

SVG + React

如果当前项目不是 React，则使用当前项目的框架。

新闻树推荐使用 SVG。

原因：

SVG 非常适合：

树枝 Path

新闻节点

文字

点击事件

Hover

动画

缩放

拖拽

SVG 可以让整个新闻树保持清晰的二维结构。

---

# 26. SVG 结构建议

可以类似：

```text
<svg>

  <g id="tree">

    <g id="trunk">
      树干
    </g>

    <g id="branches">

      <g id="branch-category-ai">
        主枝
        分枝

        <g class="news-leaf">
          新闻
        </g>

        <g class="news-leaf">
          新闻
        </g>

      </g>

    </g>

  </g>

</svg>
```

具体组件根据当前项目技术栈决定。

---

# 27. 组件建议

根据当前代码架构合理拆分。

可以参考：

```text
NewsTree
TreeTrunk
TreeBranch
NewsLeaf
NewsCard
NewsTreeControls
CategoryNavigation
ViewSwitcher
NewsList
NewsDataTransformer
Background
```

不要为了组件数量而拆分。

应该按照实际职责拆分。

---

# 28. 性能要求

新闻可能达到：

50条

100条

200条

甚至更多。

必须考虑性能。

不能出现：

新闻一多页面就卡顿。

SVG 节点过多时，需要控制 DOM 数量。

不要每次鼠标移动都重新计算整棵树。

树的位置计算与 UI 状态分离。

新闻数据变化时，只重新计算必要部分。

---

# 29. 响应式

桌面端是主要场景。

但必须兼容：

笔记本

平板

手机

手机端不能简单缩放桌面版本。

移动端重点保证：

树能够完整显示

可以拖动

可以缩放

可以点击新闻

分类导航可以横向滚动

新闻信息卡片不会超出屏幕

---

# 30. 不要破坏现有功能

以下功能必须保留：

新闻获取

新闻分类

新闻来源

新闻时间

新闻 URL

新闻详情

数据刷新

现有 API

现有数据处理逻辑

如果当前项目存在缓存机制，也不要因为 UI 重构而删除。

本次主要改造：

展示层

交互层

页面结构

不要随便修改后端。

---

# 31. 不要直接复制 NewsNow

参考 NewsNow 的原因是：

学习优秀的新闻聚合方式。

学习：

新闻组织

分类

信息密度

数据获取

阅读体验

但是最终产品不能只是：

NewsNow + 一棵树背景。

必须真正形成：

新闻树信息可视化产品。

---

# 32. 开发顺序

请严格按照以下顺序开发。

## 第一步：阅读项目

先完整阅读：

```text
package.json
src
components
pages
api
data
services
```

以及项目实际存在的其他目录。

理解：

当前框架

页面结构

新闻数据

API

分类

跳转

样式

---

## 第二步：生成技术方案

先不要立即重写代码。

先分析：

当前项目哪些代码可以复用。

哪些组件需要修改。

哪些组件需要新增。

新闻数据如何转换成树。

树的布局算法如何实现。

SVG 如何实现。

如何实现拖拽。

如何实现缩放。

如何解决新闻标题碰撞。

---

## 第三步：先完成静态新闻树

先不要增加粒子。

先实现：

树干

主枝

分枝

新闻叶片

确保页面第一眼就是一棵完整的树。

---

## 第四步：接入真实新闻数据

把当前项目真实新闻数据显示到树上。

确保：

分类正确。

标题正确。

来源正确。

发布时间正确。

URL 正确。

---

## 第五步：实现交互

依次实现：

Hover

Click

Drag

Zoom

Reset

---

## 第六步：实现分类

底部分类切换。

确保树根据分类重新布局。

---

## 第七步：实现列表模式

列表与树使用同一份新闻数据。

不能维护两套新闻数据。

---

## 第八步：实现视觉优化

最后再增加：

动画

背景

轻微环境效果

渐变

阴影

细节优化

不要在项目一开始就堆特效。

---

# 33. 最终验收标准

启动项目。

进入首页。

第一眼必须看到：

**一棵树。**

而不是：

粒子。

节点网络。

散落的圆点。

星空。

关系图。

页面中央应该明确存在：

树干

主枝

分枝

新闻叶片。

新闻叶片必须真实对应新闻数据。

用户能够通过树理解：

新闻分类

新闻数量

新闻之间的层级关系。

用户可以：

悬停新闻

查看新闻信息

点击新闻

打开新闻

拖动树

缩放树

切换分类

切换列表模式。

列表模式也必须正常工作。

---

# 34. 最重要的视觉验收

完成后，请自己检查：

如果把所有新闻文字隐藏，只保留树的结构，

页面看起来是否仍然是一棵树？

如果答案是否定的：

说明实现失败。

如果页面看起来只是：

```text
·  ·  ·   ·
  ·   ·
·   ·   ·
```

说明实现失败。

如果页面看起来像：

```text
    ─────●
 ────●
─────●────●
    │
    ●
```

说明实现失败。

正确效果应该接近：

```text
                    新闻
                  /
          新闻 ──/
                /
        ────────
             \
              \──── 新闻

                    │
              ──────┤──────
             /      │       \
         新闻       新闻      新闻
          /         │         \
       新闻        新闻       新闻
                     │
                     │
                    树干
                     │
                     │
                    树根
```

最终页面必须具有非常清晰的：

**树的轮廓 + 树枝结构 + 新闻挂载关系。**

---

# 35. 最后要求

在开始修改代码之前：

先阅读整个项目。

然后输出：

1. 当前项目技术架构分析
2. 当前新闻数据结构
3. 当前页面结构
4. 新闻树实现方案
5. 需要修改的文件
6. 需要新增的文件

确认方案合理之后，直接开始修改。

不要为了实现视觉效果重写整个项目。

不要删除现有功能。

不要使用 Mock 数据作为最终实现。

不要使用随机散点模拟树。

不要使用粒子网络模拟树。

不要把新闻做成漂浮在树周围的独立卡片。

必须通过：

**树干 → 主枝 → 分枝 → 新闻叶片**

建立完整的视觉和数据层级关系。

最终产品应该是一棵真正可交互的：

**新闻树。**