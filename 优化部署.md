# NewsTree 项目升级与部署任务

项目：
https://github.com/Learner-ning/news-nb

参考：
https://github.com/ourongxing/newsnow

生产域名：
https://newstree.dpdns.org

## 核心目标

将当前 news-nb 改造成真正可用的“新闻树”新闻聚合网站。

不要把新闻树做成粒子网络、散落发光点、知识图谱或随机节点。

页面中央必须是一棵完整的二维树：

树干 → 主枝 → 分枝 → 新闻叶片

新闻叶片必须真实绑定新闻数据，而不是 Mock 数据。

推荐使用当前项目已有技术栈 + SVG 实现，不要无必要引入 Three.js。

## 新闻树逻辑

树干：新闻整体。

主枝：新闻分类。

分枝：新闻来源或新闻主题。

叶片：具体新闻。

根据真实新闻数据动态生成树。

禁止使用随机坐标生成新闻。

新闻位置必须稳定、可预测，并避免大量重叠。

## 新闻热度

不要简单按照爬取先后排序。

新增独立的 `calculateHeatScore()`，综合考虑：

发布时间
时间衰减
新闻来源权重
多来源重复报道
分类热度
关键词热度

有多个来源报道的同一事件应该获得更高热度。

热度影响新闻叶片大小、位置和视觉权重。

高热度新闻更突出，低热度新闻降低视觉权重。

## 新闻聚合

建立统一新闻数据结构：

title
url
source
category
publishedAt
summary
image
tags
heatScore

增加基础去重。

尽量识别同一事件的重复新闻。

后续可扩展 AI 新闻主题聚类，但本次不要过度复杂化。

## 新闻源

先读取现有新闻源实现。

在不破坏现有数据层的情况下增加高质量新闻源。

重点扩展：

AI
科技
互联网
财经
国际
国内
体育
娱乐
汽车
游戏
科学
开发者

建立统一 source adapter，不要把所有爬虫逻辑写在一个文件中。

新闻源应该可以：

启用
关闭
设置分类
设置权重

优先使用 RSS、公开 API 或稳定的数据来源。

## 页面

默认进入“新闻树”。

中央：

完整新闻树。

左侧：

新闻树
新闻列表
热榜

底部：

全部
AI
科技
财经
国际
国内
体育
娱乐
其他实际存在分类

分类切换后重新计算树布局。

## 新闻交互

支持：

Hover
Click
Drag
Zoom
Reset

Hover 新闻时显示：

新闻标题
来源
发布时间
热度

点击新闻使用现有项目的详情页或 URL 跳转逻辑。

不要重新建立第二套新闻详情系统。

## 新闻列表

列表模式使用真实新闻数据。

支持：

热度排序
时间排序
分类筛选

默认使用：

热度 + 时间衰减

## 热榜

增加简单的 TOP 新闻列表。

例如：

TOP 10

1. 新闻
2. 新闻
3. 新闻

使用统一的 `heatScore`。

## 视觉

整体：

现代
简洁
沉浸式
科技感
信息可视化

背景可以使用深色夜晚风格。

可以有少量星空和环境粒子。

但粒子只能作为背景。

不能用粒子组成新闻树。

即使关闭所有特效，页面仍然必须明显是一棵树。

## 动画

树第一次加载：

树干 → 主枝 → 分枝 → 新闻叶片

依次出现。

分类切换和新闻刷新使用平滑过渡。

不要使用夸张动画。

## 响应式

桌面端优先。

同时兼容手机和平板。

移动端支持：

拖拽
缩放
点击新闻
分类横向滚动

## 性能

不要大量创建复杂 DOM。

不要每次 mousemove 都重新渲染整棵树。

树布局和数据处理独立。

避免重复事件监听。

保证 50～200 条新闻时仍然流畅。

## Cloudflare 部署

检查当前项目现在的部署方式。

目标：

GitHub → Cloudflare → newstree.dpdns.org

配置 GitHub 自动部署。

以后修改代码后：

git commit
git push

Cloudflare 自动构建并发布。

不要要求我每次手动上传网站文件。

检查：

Build Command
Output Directory
Node 版本
Environment Variables
DNS
SSL
自定义域名

不要把 API Key、Token、Secret 写进代码。

## 开发流程

第一步：

读取整个项目，理解技术栈、目录、新闻源、数据结构、API、分类、详情页和当前部署方式。

第二步：

检查当前新闻源数量和实现方式。

第三步：

先实现真实的 SVG 新闻树：

树干
主枝
分枝
新闻叶片

不要先做粒子和特效。

第四步：

接入真实新闻数据。

第五步：

增加去重和 heatScore。

第六步：

增加分类切换。

第七步：

增加列表模式和热榜。

第八步：

增加 Hover、Click、Drag、Zoom。

第九步：

优化视觉和动画。

第十步：

本地 build 和测试。

第十一步：

配置 GitHub → Cloudflare 自动部署。

第十二步：

验证：

https://newstree.dpdns.org

## 严格限制

禁止：

随机散点模拟树
粒子网络模拟树
新闻随机漂浮
大量发光圆点
Mock 数据作为最终功能
重写整个项目
破坏现有新闻 API
破坏现有新闻详情
删除已有功能
把热度逻辑写进 UI 组件
把 API Key 提交到 GitHub

## 最终要求

开始前先分析项目，但不要停在分析阶段。

分析完成后直接修改代码、运行、测试和修复。

最终打开网站时必须第一眼看到：

一棵完整的新闻树。

新闻真实来自新闻源。

新闻分类形成主枝。

新闻来源或主题形成分枝。

具体新闻形成叶片。

热度决定新闻视觉权重。

用户可以探索新闻树，也可以切换列表和热榜。

最终通过：

GitHub
↓
Cloudflare
↓
https://newstree.dpdns.org

实现自动部署。