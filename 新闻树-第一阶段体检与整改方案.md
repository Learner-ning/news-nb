# 新闻树（news-nb）第一阶段：代码体检与整改方案

> 体检日期：2026-09-16
> 体检对象：`Learner-ning/news-nb`（本地工作区 `D:\wr new\新闻树`，HEAD = `a12475c`）
> 参照项目：`ourongxing/newsnow`（仅借鉴数据源组织 / 缓存 / 部署 / 结构，不借鉴 UI，不改造成 NewsNow）
> **本报告只做检查与分析，未修改任何项目代码。** 所有结论均来自源码通读 + 本机实跑探针（真实抓取、真实布局计算、真实接口计时）。

---

## 〇、体检方法（可复核）

| 手段 | 内容 |
|---|---|
| 源码通读 | `server.js`(78) `src/index.js`(75) `lib/news-core.mjs`(520) `public/js/*.js`(1413) `public/index.html`(178) `public/style.css`(634) `wrangler.jsonc` `package.json` `DEPLOY.md` `README.md` `优化部署.md` + 构建日志 |
| 真实抓取探针 | 直接 import `lib/news-core.mjs` 调用 `collectAll()`，统计每源成败、条目数、字段一致性、空值、重复、热度分布 |
| 布局复算探针 | 按 `tree-view.js: layout()` 逐行复刻几何计算，用真实数据算出包围盒、`fit()` 缩放、叶片可读字数、SVG 元素数 |
| 接口实测 | 本地启动 `server.js`，测冷/暖耗时、响应头、载荷构成 |
| 环境核验 | `git status/diff/log/remote`、`package-lock.json`、`node_modules`、构建日志 |

关键实测数字（后文反复引用）：

| 指标 | 实测值 |
|---|---|
| 单轮抓取条目 | **209 条**（RSS 109 + 热榜 100） |
| 冷启动接口耗时 | **30.18 s**（`/api/news?refresh=1`）／暖缓存 **0.010 s** |
| 响应载荷 | **468,366 B**，其中 `content` 占 **294,478 B（63%）**，平均单条 2,414 B |
| 树包围盒 | **8992 × 1342**（宽高比 **6.70 : 1**） |
| `fit()` 后缩放 | 1440×900 → **0.155**；1920×1080 → 0.209；390×844 → **0.038** |
| 叶片字号实际屏幕像素 | 桌面 **2.0 px**；手机 **0.5 px** |
| 叶片标题可显示字数 | 平均 **11.3 字 / 标题平均 24.8 字 = 55%**，最差 21% |
| 含裸露 HTML 标签的条目 | **54 / 209**（IT之家 15、少数派 10、GitHub热榜 14、雪球 15） |
| 无真实发布时间条目 | **69 / 209（33%）** |
| 多源合并（去重生效）条目 | **0 条** |
| heatScore 不同取值 | 53 个 / 209 条，最高 0.645，中位 0.356 |

---

## 一、A. 当前项目存在的问题（按 22 个检查点归口）

### 检查点 1｜整体技术架构

**现状（这部分是优点，先说清楚）**：三层结构清晰 —— 前端原生 ESM（零框架零构建）→ 服务端双实现（Express 本地 / Cloudflare Worker 生产）→ 统一数据引擎 `lib/news-core.mjs`（抓取/解析/去重/热度）。`TreeView` 内部也分得干净：`layout()` 纯几何、`build()` 只渲染、`attach()` 只管交互。

**问题**：
1. **服务端逻辑双份漂移**（P1）：`server.js:14-68` 与 `src/index.js:18-46` 是同一套 API 的两份实现，且能力不对等 —— 本地有 `stale` 兜底（`server.js:18-20`），**生产 Worker 没有**；TTL 本地 5 分钟、Worker 硬编码 2 分钟（`src/index.js:9`）。**本地容错强于生产，方向是反的。**
2. **无测试 / 无 lint / 无 CI**（P1）：`package.json` 只有 `start`/`dev`，而部署是 `git push` 即上线。
3. **源定义与解析耦合在一个文件**（P2）：9 个 RSS + 7 个热榜全部写在 `news-core.mjs`，`parseHot()` 是一段 90 行的 `if/else` 链；新增源必须改公共文件 → 「一个源腐坏」的影响半径被放大。

### 检查点 2｜新闻数据获取流程

`collectAll()` → `Promise.all` 并行抓 9 RSS + 7 热榜（微博额外先 warmup 1 次）→ `parseFeedXml()` / `parseHot()` → `dedupe()` → `calculateHeatScore()` → 输出 items。

**问题**：
1. **无 single-flight**（P1）：`server.js getNews()` 与 `src/index.js loadAll()` 都只判断"缓存是否新鲜"，没有"进行中请求"的合并。冷缓存下并发 N 个请求 → 触发 **17 × N** 次上游抓取。
2. **`refresh=1` 无节流**（P1）：任何人反复点刷新都会触发全量 17 次上游抓取，对上游不友好，也是自伤。
3. **抓取与请求同步耦合**（P0）：用户请求必须等抓取完成（实测 30 s），没有"后台预热 + 请求只读缓存"的分离。

### 检查点 3｜新闻源是否稳定

**实测：16 个启用源中，不同轮次失败 1 ~ 8 个。** 逐源探测（串行，带真实 UA）：

| 源 | 结果 | 源 | 结果 |
|---|---|---|---|
| Solidot | ECONNRESET | 微博 | 200（需 warmup，有轮次失败） |
| IT之家 | 200 | 腾讯新闻 | 200 |
| 少数派 | ECONNRESET | 今日头条 | 200 |
| 量子位 | UND_ERR_CONNECT_TIMEOUT | 抖音 | 200（有轮次失败） |
| 爱范儿 | 200 | 哔哩哔哩 | 200 |
| GitHub热榜 | 200 | 百度贴吧 | 200（有轮次失败） |
| 触乐 | ECONNRESET | 澎湃 | 200 |
| 华尔街见闻 | ECONNRESET | 雪球话题 | 200（有轮次失败） |

**问题**：同一批源在不同轮次成功/失败交替（雪球、微博、抖音、贴吧都出现过两态），说明**源可用性是抖动的常态**，而不是偶发。而代码对此的应对只有一句 `errors.push`。

另：`PLANNED_SOURCES` 诚实登记了 7 个不可达源（知乎 401 / 虎扑 404 / 懂球帝 403 / 豆瓣反爬 / 36氪 反爬 / 掘金签名 / 酷安等），**这一点做得比参照项目更诚实**，应保留。

### 检查点 4｜新闻源失败后的处理

**问题**：
1. `fetchText()` 单次尝试，**无重试、无退避**（`news-core.mjs:95-109`，15 s 超时后直接抛）。
2. 失败只记 `{source, message}`，**前端只在顶栏显示"部分源不可用"**，不告诉用户是哪个源、什么原因（`app.js:136`）。
3. **Worker 侧没有 stale 兜底**：一旦某源全挂且缓存已冷，用户直接看到空树（本地 Express 有兜底）。
4. 无"连续失败降频 / 自动熔断"，坏源每轮都会再等一次超时。

### 检查点 5｜新闻数据是否存在重复

**实测：完全同标题残留 0 组，URL 重复 0 条 —— 基础去重有效。但更严重的是"去重从不触发"：**

| 项 | 实测 |
|---|---|
| `dupCount > 1`（被判为同一事件）的条目 | **0 / 209** |
| 跨源合并条目 | **0** |

**问题**：`dedupe()` 的相似度阈值 0.94（`news-core.mjs:371`）在当前源组合下**从未命中**，于是 `heatScore` 里"多源重复报道加权"这一项**永远是 0**，原始需求「有多个来源报道的同一事件应该获得更高热度」**实际上没有实现**。此外 `dedupe()` 是**跨 kind 全局去重**：热榜条目与新闻条目一起比对，理论上一条热搜可能被同题新闻"吞掉"并从热榜视图消失（当前因 0 命中未暴露，属潜在缺陷）。

### 检查点 6｜标题、来源、时间、URL 等字段是否统一

字段集合实测（25 个键，无缺字段）：`id, kind, type, category, tag, platform, title, url, source, summary, content, image, publishedAt, time, hasRealTime, fetchedAt, hotRank, rank, hotTag, praise, discuss, dupCount, dupSources, heatScore, heatPct`

**问题**：
1. **五组同义字段双写**（P2）：`kind`/`type`、`category`/`tag`、`source`/`platform`、`publishedAt`/`time`、`rank`/`hotRank`。同一语义两条路径，改一处忘一处必然漂移。
2. **`summary`/`content` 泄漏裸露 HTML**（P0，详见下节 P0-1）——这是"字段不统一"最严重的表现：同一字段在不同源下分别是"纯文本"和"HTML 源码"。
3. **`image` 恒为 null**（209/209）：RSS 的 `<img>` 明明存在（实测 IT之家 content 内带 `https://img.ithome.com/...jpg`），但没有任何抽取逻辑 → 需求中的 `image` 字段形同虚设。
4. **需求中的 `tags` 字段不存在**（只有单值 `tag`）。
5. **`hotTag`/`discuss` 恒为 null**（209/209）、`praise` 仅 15 条有值 → 平台特有字段只在单源生效，未做通用化。
6. `heatPct` 计算了但全项目无人使用（死字段）。

### 检查点 7｜新闻分类是否合理

**问题**：
1. **声明 5 类，实际只出现 3 类**：`CATEGORIES = ["科技","体育","国内","影视","财经"]`（`news-core.mjs:18`），实测只出现 **科技 79 / 国内 100 / 财经 30**；**体育没有任何源**，影视只有触乐 1 个源（本轮抓取失败）→ 主枝直接缺失。
2. **分类与源分布严重失衡**：国内 7 源 100 条、科技 6 源 79 条、财经 2 源 30 条 → 树的主枝粗细/高度极不均（见检查点 11）。
3. **语义混杂**：7 个热榜平台（微博/腾讯/头条/抖音/B站/贴吧/澎湃）全部归入"国内"，把"平台话题榜"和"新闻"放在同一个主枝下，读者无法区分信息类型。
4. **三处分类顺序定义互不相同**（P2）：`CATEGORIES`（lib）/ `TAG_ORDER`（helpers.js:76）/ `TAG_PRIORITY`（news-store.js:96，含 综合/数码/AI/国际/娱乐 等永远不会出现的分类）。新增分类要改三处。
5. `CATEGORIES` 导出后**无人消费**（死导出）。

### 检查点 8｜热度排序机制

`calculateHeatScore()`（`news-core.mjs:395-445`）权重：`0.30×fresh + 0.28×quality + 0.14×rankBonus + 0.18×dup + 0.08×catFreq + kw`。

**问题（有量化证据）**：
1. **权重和 = 1.10 > 1**，被 `Math.min(1, …)` 截断 → 高分区间被压平。
2. **28% 权重的"来源权重"几乎没有区分度**：`quality = min(1, 0.55 + w×0.42)`，`w ∈ [0.9, 1.15]` → `quality ∈ [0.928, 1.0]`，**0.28 的权重只带来 0.02 的分差**。
3. **8% 权重的"分类热度"贡献上限 0.0048**（`catFreq ≤ 0.06`），可忽略。
4. **18% 权重的"多源重复"实际贡献 0**（见检查点 5）。
5. 结果：实测 heatScore 只有 **53 个不同值 / 209 条**，最高 0.645、中位 0.356；**热度前 20 名中 14 条来自 IT之家**（发布最频繁的源）→ 排序实质由"是否有真实时间 + 时间衰减 + 关键词"决定。
6. `heatPct` 把 0~1 的分数当百分比展示（"热度 37%"），**语义不明**：它不是概率、不是占比，用户无从理解。
7. **热度只影响叶片宽度，不影响位置**（`tree-view.js:88-89` 改宽度；`layout()` 的排布顺序 = API 返回顺序），原始需求"热度影响新闻叶片大小、位置和视觉权重"只做到 1/3。

### 检查点 9｜新闻树的数据结构是否合理

`buildTreeModel()`（`news-store.js:61-94`）产出 `categories[] → sources[] → items[]`，三层嵌套与"主枝→分枝→叶片"一致，**方向正确**。问题：
1. 结构里**没有"树干"这一层的任何数据表达**（树干位置靠 `layout()` 用加权重心 `crownX` 现算），树干与数据无映射关系。
2. `sources` 只按 `item.source` 字符串分组，**热榜平台与 RSS 站点混在同一层**，无法表达"来源类型"。
3. `count`/`total` 冗余计算两次（`cat.total++` 与 `cat.count`）。
4. 分类顺序用 `TAG_PRIORITY`（`news-store.js:96`）二次排序，与后端 `CATEGORIES` 不一致。

### 检查点 10｜树干、主枝、分枝、新闻叶片之间的数据映射

| 视觉层 | 数据映射 | 评价 |
|---|---|---|
| 树干 | 无映射（`crownX` = 各主枝 x 按条目数加权平均，`crownY` = 最深主枝 + 128） | ⚠️ 树干是"算出来的坐标"，不承载数据 |
| 主枝 | `category`（= `tag`） | ✅ 正确，但只有 3 个 |
| 分枝 | `source`（= `platform`） | ✅ 正确，但热榜与新闻同层 |
| 叶片 | 单条 `item`，宽度受 `heatScore` 影响 | ⚠️ 位置不受热度影响；文字平均只显示 55% |

**问题**：映射是"结构性正确但语义欠表达"——树干缺数据语义、分枝缺类型区分、叶片位置与热度无关。

### 检查点 11｜树形布局算法（**本次最严重的问题**）

复刻 `layout()` 用真实 209 条数据计算：

```
主枝 科技:  79 条, 6 源, 分枝排数 [6,8,7,5,4,5], 分枝末端 y=948
主枝 财经:  30 条, 2 源, 分枝排数 [7,5],          分枝末端 y=852
主枝 国内: 100 条, 7 源, 分枝排数 [4,5,5,4,5,6,3], 分枝末端 y=756
包围盒: 8992 × 1342   宽高比 6.70 : 1
树干高 216 px（树冠宽 8992 px）
每排宽度: min 161 / max 555 / avg 447，共 79 排
SVG 元素约 890 个
```

**问题**：
1. **树被拉成"横放的拖把"**：主枝沿 X 轴线性平铺（`CAT_GAP=150` + 各分类自身宽度累加），分类越多越宽；纵向却只有 1342 px。宽高比 6.7:1，与"树"的视觉直觉（纵向生长、横向收拢）相反。
2. **`fit()` 把整树硬缩到不可读**：`s = min((w-44)/8992, (h-70)/1342, 1.7)` → 1440×900 下 **s = 0.155** → 13 px 的叶片字渲染成 **2.0 px**、叶片宽 24 px。**用户第一眼看到的是一棵"几乎读不出字的树"**，必须手动放大；放大后又移出视口。
3. **手机端彻底不可用**：390×844 → s = 0.038 → 字号 **0.5 px**，整树高 52 px。
4. **切到单分类也不解决**：只显示"科技"时包围盒 6174×958 → s = 0.226 → 字号 2.9 px，仍不可读。
5. **`rowCap` 逻辑与视觉目标冲突**：`cats.length <= 1 ? 1020 : 560` —— 全树时每排仅 560 px，把每个来源切成 4~8 排，纵向堆叠，反而让树"更宽更扁"。
6. **缺少语义缩放（LOD）**：缩小时没有"隐藏文字/按来源聚类"的降级策略，只有硬缩。
7. 树干 216 px vs 树冠 8992 px，比例失真，视觉上不像树干像横杆。

### 检查点 12｜Hover / Click / Drag / Zoom 是否存在问题

**存在，共 8 处：**

| # | 问题 | 位置 | 严重度 |
|---|---|---|---|
| 1 | **双指缩放结束后单指拖动会"跳变"**：`pointerup` 只在自己是 `drag.id` 时清空 `drag`；第二根手指抬起时 `drag` 残留旧起点，剩余手指一动，树就跳到手指位置 | `tree-view.js:421-428` | P2 |
| 2 | **`window.resize` 无条件 `tree.fit()`**：移动端地址栏收缩/展开会触发 resize → 用户正在浏览的缩放与位置被重置 | `app.js:456-458` | P2 |
| 3 | **触摸端 hover 语义不成立**：悬停卡只由 `pointerover` 触发 | `tree-view.js:433` | P2 |
| 4 | **`touch-action: none` 加在整个 SVG + `body{overflow:hidden}`** → 移动端在树区域无法滚动页面 | `style.css:45,206` | P2 |
| 5 | **移动端操作提示被隐藏**：`.tree-hint{display:none}`（≤860px），用户不知道可以拖拽/缩放 | `style.css:462` | P2 |
| 6 | **`#zoom-fit` 与 `#zoom-reset` 绑定同一个函数**（都调 `tree.fit(true)`），两个按钮功能完全等同 | `app.js:383-384` | P2 |
| 7 | **树叶片完全无法键盘访问**：SVG `<g>` 无 `tabindex`、无方向键遍历、无焦点样式 | `tree-view.js:266-283` | P2 |
| 8 | **悬停卡位置不随缩放/滚动更新**：定位用一次性 `clientX/Y`，滚轮缩放后卡片停在旧屏幕位置 | `views.js:161-170` | P2 |

做对的地方（保留）：拖拽与点击用 `_suppressClick` 区分（>4 px 才算拖拽）；`pointer capture` 处理了 click 目标漂移；缩放以指针为锚点（`zoomBy` 数学正确）；叶片悬停会高亮所属主枝/分枝/叶柄（`.on` 类）。

### 检查点 13｜新闻详情展示逻辑

**问题**：
1. **详情页直接显示裸露 HTML 标签**（P0，见 P0-1）：`views.js:127` 用 `esc(content)` 转义后放进 `white-space: pre-wrap` 的 `.detail-body`，实测 54/209 条会渲染出一屏 `<p data-vmark="7c5b">…` 文本。
2. **15 条正文被截断在标签中间**（`content.slice(0,2000)`），显示效果更破碎。
3. 热榜条目的 `content` 是模板文案（"本条为「微博」第 3 位热门话题（抓取于 …）"），详情页信息量≈0。
4. **深链会失效**：`/detail/:id` 依赖"该条仍在最近一批数据里"；服务端不持久化，超过一个缓存周期后返回 404「新闻不存在或已过期」→ 分享出去的链接会失效。
5. 无"上一条/下一条"，看完必须返回列表重新定位。
6. `document.title` 客户端改写，SPA 无 SSR/无 meta 注入 → 分享到社交平台没有标题与摘要。

### 检查点 14｜PC 端和移动端适配

**PC 端**：布局与交互基本可用（除"树默认不可读"外）。

**移动端问题**：
1. 树 `fit` 后字号 0.5 px —— 等于没有树（见检查点 11）。
2. 左 dock（`left: 8px`，`max-width:860px` 只缩小不隐藏）压在树左侧内容上。
3. 操作提示隐藏（`.tree-hint`），无替代提示。
4. 树区域 `touch-action:none` 阻断页面滚动；`body{overflow:hidden}` + `#stage{position:fixed}` 使列表/热榜只能内部滚动（可用，但树视图完全不能滚动）。
5. 无 `prefers-reduced-motion`（生长动画 + 星辰背景在低端机上是纯负担）。
6. 无 PWA/manifest，无离线。

### 检查点 15｜页面加载速度

| 环节 | 实测 |
|---|---|
| 冷启动接口 | **30.18 s**（17 个上游请求，最慢单源 10.5 s） |
| 暖缓存接口 | 0.010 s |
| 响应载荷 | 468,366 B（`content` 占 63%） |
| 静态资源合计 | 约 86 KB（index.html 8 KB + style.css 28.8 KB + 5 个 JS 共 49.8 KB），无构建、无压缩（本地） |
| 首屏可见内容 | **30 s 内为空白**（树被 `no-tree` 置 `visibility:hidden`，无骨架屏、无旧数据） |

**问题**：① 冷启动 30 s 空白；② 载荷 63% 是无用正文；③ 每次访问都全量重取（前端无持久化缓存）；④ 无骨架屏/进度反馈。

### 检查点 16｜API 请求数量

| 层 | 数量 |
|---|---|
| 前端每次进首页 | **1 次** `/api/news`（进详情页 0 次额外请求，因为数据已在列表里） |
| 服务端每次冷缓存 | **17 次**上游请求（9 RSS + 7 热榜 + 微博 warmup 1） |
| 并发 N 个请求遇到冷缓存 | **17 × N 次**（无 single-flight） |
| 每次点刷新 | 再 17 次（无节流） |

**结论**：前端请求数已经很少（设计良好）；**问题全在服务端缓存与并发控制**。

### 检查点 17｜缓存机制

**问题**：
1. **两套不一致**：本地 5 分钟（`CACHE_TTL_MS`）、Worker 2 分钟（硬编码 `TTL = 120000`）。
2. **全局统一 TTL，无源级差异**：日更的 Solidot 与秒级的微博热搜用同一个刷新周期（参照项目 NewsNow 是"全局 TTL + 逐源 interval"双窗口，本项目两者都没有）。
3. **Worker 缓存是 isolate 内存**：`let cache = null`，无 KV / 无 Cache API → 冷 isolate、多边缘节点各自抓一遍。
4. **`Cache-Control: no-store`**（`src/index.js:14`）→ CDN 与浏览器都无法缓存。
5. **无 single-flight**（见检查点 16）。
6. **前端零持久化**：`news-store.js` 的 `state` 是纯内存，刷新页面即清空。
7. **无负缓存**：抓取全失败时返回空数组，下一个请求继续全量重抓。

### 检查点 18｜错误处理

**问题**：
1. **错误结构不一致**：服务端是 `{source, message}`，前端 fetch 失败时是 `[e.message]` 字符串（`news-store.js:28`）→ 前端消费 `errors` 时字段类型不定。
2. **错误不可见**：只有顶栏一句"部分源不可用"，不显示源名与原因（`errors` 已经拿到却不用）。
3. **无重试、无退避、无 Toast**：失败与成功在 UI 上几乎无区别。
4. **前端 10 余处空 `catch {}`**（`app.js` 的 `loadBg/save/saveBg/状态初始化/fileToDataUrl/probe` 等）→ 上传图片失败、localStorage 写满、背景 URL 无效时**"点了没反应"**。
5. 无 `error`/`unhandledrejection` 全局兜底。

### 检查点 19｜空数据处理

**做得基本到位（保留）**：树空 → `no-tree` + 提示文案（`app.js:180-183`）；列表空 → 「暂时没有新闻，请稍后刷新。」（`views.js:29-32`）；热榜空 → 「当前分类暂无热搜数据…」（`views.js:74-77`）；详情 404 → 错误页 + 返回按钮（`views.js:103-110`）。

**不足**：
1. 三种"空"没有区分原因（网络失败 / 全部源失败 / 真的没有数据），文案一律"请稍后刷新"。
2. 树空时 `visibility:hidden` 会让舞台看起来像"页面坏了"，而不是"数据加载失败"。
3. `errors` 非空但 `items` 非空时，用户无从知道"少了几条"。

### 检查点 20｜重复、临时方案、Mock 数据、废弃代码

**Mock 数据：无。** 全部来自真实抓取，`PLANNED_SOURCES` 也只登记不伪造 —— **这是本项目最值得保留的纪律**（唯一"合成内容"是热榜条目的 `content` 模板文案，属说明性文字，不冒充新闻正文）。

**重复 / 废弃清单（实测）**：

| 类型 | 项 |
|---|---|
| 死函数（全项目 0 引用） | `helpers.js`: `slugify`、`fmtClock`、`el`、`clamp`（仅被同样死掉的 `leafWidthFor` 用）；`news-store.js`: `leafWidthFor`、`leafMeta`、`findItem`；`tree-view.js`: `getLeaf` |
| 死导出 | `news-core.mjs`: `CATEGORIES`、`BOARD_TOP_PER_PLATFORM`（热榜 Top10 在 `views.js:81` 又硬编码了一次 `limit: 10`） |
| 死字段 | `heatPct`（算了没人用）、`dupSources`（只输出不消费） |
| 重复实现 | `views.js:5 heatText()` 与 `views.js:55-69 hotRow()` 内联文案；叶片宽度有 3 套算法（`tree-view.leafW` 实际生效 / `news-store.leafWidthFor` 死代码 / `truncateByWidth` 用 textWidth） |
| 死 CSS | `.card`（8 条规则）、`.card .tag/.title/.summary/.foot`、`.card.empty`、`.lg-cards`（含 520px 媒体查询内 1 条）、`.b-dup`、`.hc-dup`、`.leaf-meta`；`.leaf-title{font-size:12.6px}` 被 500 行的 `13px` 覆盖 |
| CSS 结构问题 | 追加式堆叠：`476 纯标题列表行` → `499 纯标题叶片字号微调` → `502 热度视觉权重` → `546 悬停卡热度行` 层层覆盖，同一声明散落多处 |
| 临时文件 | `fix-verification.html`（24.6 KB 验证页）、`news-tree-review-report.md`（34.9 KB 上一轮报告）、`优化部署.md`、1.6 MB 壁纸 PNG、构建日志 —— 全部堆在仓库根目录且未跟踪未忽略 |

### 检查点 21｜Cloudflare 部署相关问题

**现状**：`wrangler.jsonc` 是 Workers Static Assets 形态（`main: ./src/index.js` + `assets.directory: ./public`），构建日志显示部署走的是 Cloudflare 的 Git 集成（`Executing user deploy command: npx wrangler deploy`）。

**问题**：
1. **`wrangler` 未固定版本**：`package.json` 无 `devDependencies`、无 `deploy` 脚本；构建日志明确记录 `npm warn exec The following package was not found and will be installed: wrangler@4.129.0` → **每次构建现下最新版 wrangler**，上游一 breaking change 就会炸构建。
2. **历史构建失败记录**：`newstree.production.*.build.log` 显示 2026-09-07 那次部署失败（`Invalid _redirects configuration: Infinite loop detected`）。当前 `public/` 已无 `_redirects`、无 `functions/`（已修），但**失败日志仍留在仓库根目录**，且无 CI 能在下次拦住同类问题。
3. **部署形态无法从仓库自证**：`DEPLOY.md` 同时出现 Pages 术语（"Output directory: public"）与 Workers 术语（"Build command: npx wrangler deploy"），读者无法判断当前到底是 Pages 项目还是 Workers 项目。
4. **无回滚与预览说明**：没有版本回滚步骤、没有 staging/预览环境约定。
5. **SPA 回落过于宽松**：`src/index.js:61-73` 对任何非 `/api/` 且非静态资源的路径都返回 `index.html` 且状态 200 → 所有不存在的路径都是软 404，不利于 SEO 与错误排查。
6. `compatibility_date: 2024-11-01`（较旧），`observability` 已开（好）。
7. 无 `run_worker_first` / 无 `not_found_handling` 配置，SPA 回落完全靠手写 Worker 逻辑（可行，但与 `assets` 能力重复）。
8. **无任何 Secret**：已核查源码、`wrangler.jsonc`、`.gitignore` —— 无 Token/Key 泄漏，`.gitignore` 覆盖 `.env*`。**此项通过。**

### 检查点 22｜GitHub 自动部署是否合理

**结论：机制方向合理，但"裸奔"——缺三道防线。**

合理之处：`git push` → Cloudflare 构建 → 上线，零手动上传，符合原始要求；`wrangler.jsonc` 入库使云端无需交互配置；Workers Static Assets 形态让"一个仓库 = 一个站点"。

问题：
1. **仓库里没有 `.github/workflows`** —— "GitHub 自动部署"实际是 **Cloudflare 的 Git 集成**，不是 GitHub Actions。这不影响功能，但意味着**没有任何质量门禁**：`push` 到 `main` 即进生产，无 test / 无 lint / 无构建校验。
2. **当前工作区处于"改了没提交"状态**：`lib/news-core.mjs`、`public/js/helpers.js`、`public/js/views.js` 三个文件为 `M`（未提交），diff 内容正是"删除 `fakeTimeAgo()`、新增 `hasRealTime`"这一轮关键修复。**结论：本地已修，线上未修**（详见 P0-4）。
3. **无分支保护/无 PR 流程约定**，也没有"非 main 分支出预览"的说明。
4. 构建环境 Node 版本未锁定（日志显示 `nodejs@24.18.0`，而 `package.json` 只写 `>=20`）。

---

## 二、B. 每个问题的严重程度（P0 / P1 / P2）

### P0 —— 上线阻断 / 正确性错误（必须先修）

| 编号 | 问题 | 证据 |
|---|---|---|
| **P0-1** | **`summary`/`content` 泄漏裸露 HTML 标签，详情页与悬停卡显示源码** | 实测 54/209 条含 `<p data-vmark="…">`；`stripHtml()` 先删标签后 `decodeEntities()`，entity 转义的 HTML 逃过删除 |
| **P0-2** | **树默认视图字号仅 2 px（手机 0.5 px），"一棵树"第一眼不可读** | 包围盒 8992×1342，`fit()` 在 1440×900 得 s=0.155 → 13px×0.155=2.0px |
| **P0-3** | **冷启动 30 s 完全空白，无骨架屏、无旧数据回填** | `/api/news?refresh=1` 实测 30.18 s；期间树 `visibility:hidden` |
| **P0-4** | **关键修复未提交，生产仍在跑旧代码** | `git status` 三个文件 M；diff = 删 `fakeTimeAgo` / 加 `hasRealTime` |

### P1 —— 影响核心功能与数据可信度

| 编号 | 问题 | 证据 |
|---|---|---|
| **P1-1** | 列表接口携带 63% 无用正文（468 KB） | `content` 294,478 B / 468,366 B；三视图都不展示正文 |
| **P1-2** | 服务端双实现漂移 + Worker 无 stale + 无 single-flight + `no-store` | `server.js` vs `src/index.js`；TTL 5min vs 2min |
| **P1-3** | 源稳定性抖动且无重试/无降频/无健康反馈 | 16 源实测失败 1~8 个；`fetchText` 单次尝试 |
| **P1-4** | 去重机制实际从未生效，"多源报道加权"落空 | `dupCount>1` 条目 = 0 / 209 |
| **P1-5** | 热度权重与实际区分度脱节（28% 权重只产生 0.02 分差） | `quality∈[0.928,1.0]`；heatScore 仅 53 个不同值 |
| **P1-6** | 主枝只出现 3 个、国内一枝 100 条、热榜与新闻混枝 | 实测 科技79/国内100/财经30；体育 0 源 |
| **P1-7** | 叶片宽度算法与真实文本宽度脱节，标题平均只显示 55% | leafW=88+字数×2.8 vs 真实 13.5px/字；平均显示 11.3/24.8 字 |

### P2 —— 工程债、体验细节、可维护性

| 编号 | 问题 |
|---|---|
| **P2-1** | 死代码 / 死导出 / 死字段 / 死 CSS 共 15 项（详见检查点 20） |
| **P2-2** | 五组同义字段双写（kind/type、category/tag、source/platform、publishedAt/time、rank/hotRank） |
| **P2-3** | 三处分类顺序定义不一致（CATEGORIES / TAG_ORDER / TAG_PRIORITY） |
| **P2-4** | 交互缺陷 8 项（缩放后拖动跳变、resize 重置视图、触摸端 hover、`touch-action:none`、移动端隐藏提示、fit/reset 重复按钮、叶片无键盘访问、悬停卡位置不更新） |
| **P2-5** | 错误结构不一致、错误不可见、10 余处静默 `catch{}`、无重试/Toast |
| **P2-6** | 详情深链会失效（无持久化，超期 404）、无上一条/下一条、无 SSR/meta |
| **P2-7** | 部署链：无 CI、wrangler 未固定版本、部署形态文档矛盾、软 404、Node 版本未锁定 |
| **P2-8** | 仓库卫生：临时文件与 1.6 MB 图片堆在根目录，`.gitignore` 缺 `.wrangler/`、`.workbuddy-ai/` |
| **P2-9** | 无 `prefers-reduced-motion`、无 `aria-live`、无真浅色主题（"明亮晨曦"仍是深色卡片） |

---

## 三、C. 推荐解决方案（逐条）

### P0-1｜修 `stripHtml` 的解码顺序（根因修复，最小改动）

**根因**：`stripHtml()` 的执行顺序是「删标签 → 解码实体」。当 feed 用 entity 转义 HTML（`&lt;p&gt;`，实测 IT之家/少数派/GitHub热榜/雪球四个源如此）时，删标签阶段看到的是纯文本（无标签可删），解码阶段却把 `&lt;p&gt;` 还原成 `<p>` → 标签"复活"。

**方案**：改为「解码 → 删标签 → 再解码」两遍，并保留原有的 script/style 剥离与空白归一：
- 先 `decodeEntities()`（让转义标签现形）
- 再剥 script/style/标签
- 再 `decodeEntities()`（处理剥标签后残留的实体，如 `&nbsp;`）
- 最后 `\s+ → 空格` + trim
- 同时把 `content.slice(0, 2000)` 改为**在纯文本上截断**（截断必须在清洗之后，避免切断标签）
- 顺带在 `buildRssItems` 里抽取首个 `<img src>` 写入 `image` 字段（内容里已有图，属白捡的收益）

**验证标准**：209 条中 `/<[a-zA-Z/][^>]*>/.test(summary|content)` 命中数 = **0**；`content` 不以 `<` 或未闭合标签结尾。

### P0-2｜布局改为"径向/扇区"，并给 fit 设可读下限

**方案（两件事，缺一不可）**：
1. **布局几何**：主枝不再沿 X 轴线性平铺，改为**以树干为原点按角度分配扇区**（每个分类占一个角度扇区，扇区宽度按该分类条目数加权）；每个分类的子树在自己的扇区内"向外生长"，叶排沿扇区法向排布。目标包围盒宽高比落在 **1.0 ~ 1.8**，`fit()` 缩放能到 **0.5 以上**（叶片字号 ≥ 6 px）。
   - 保留"确定性、无随机"约束：角度分配、排布顺序全部由数据决定（`angleStart = 累计占比 × 2π`），同数据必得同树。
   - 备选（更保守、改动更小）：保留现有"主枝水平分布"结构，但把**分类改成纵向分层**（树干在中轴，分类主枝上下交错左右分布），同样能把宽高比压到 2:1 以内。
2. **语义缩放（LOD）**：`fit()` 计算出的缩放低于阈值时不硬缩 ——
   - `s < 0.42`：隐藏叶片文字，只留色块轮廓（按热度深浅）
   - `s < 0.22`：把同一来源的叶片折叠成一个"来源节点"（显示"IT之家 · 15"）
   - 缩放到可读区间（`s ≥ 0.42`）才渲染文字
   - 同时给 `fit()` 设 `minReadableScale`，若整树入镜必然低于该值，则**默认只呈现"树冠主干区"**（聚焦视口）而不是把整树缩成一条线。

**验证标准**：1440×900 默认视图叶片字号 ≥ 6 px；单分类视图 ≥ 9 px；宽高比 ≤ 1.8；同一批数据两次 `layout()` 输出完全一致（无随机）。

### P0-3｜首屏"缓存优先 + 后台更新"

**方案（四层）**：
1. **服务端**：抓取与请求解耦 —— 进程启动即预热一次；`/api/news` 只读缓存，缓存为空时**返回 202 + 空列表 + `warming: true`**，前端据此显示骨架屏并 3 s 后重试（而不是阻塞 30 s）。
2. **前端持久化**：`localStorage` 存上次成功结果（只存渲染需要的字段，见 P1-1 的瘦身结果，约 100 KB 内），启动时**先渲染旧数据**（带"缓存"标记），再后台拉新。
3. **骨架屏**：树区域在无数据时渲染占位（干+枝的灰色轮廓 + 3~5 个叶片占位块），列表渲染 6 个骨架卡片。
4. **进度反馈**：顶栏显示"正在更新…"，刷新按钮 `disabled` + 旋转，完成后 Toast。

**验证标准**：有本地缓存时首屏可见内容 < 300 ms；无缓存时 < 1.5 s 见到骨架与旧数据；接口不再有 > 5 s 的同步阻塞响应。

### P0-4｜先提交，再谈整改

**方案**：把 `lib/news-core.mjs`、`public/js/helpers.js`、`public/js/views.js` 三处"删除伪造时间戳"的修复，连同验证结果（本报告检查点 6/8 的实测数据可作证据）单独提交并推送，让线上与本地一致。**这一步应在任何新改动之前完成**，否则后续 diff 会与这轮修复混在一起，无法回滚。

### P1-1｜接口瘦身

**方案**：
- `collectAll()` 输出的列表项**不含 `content`**（保留 `summary` 前 160 字）；
- 新增 `content` 只在 `/api/news/:id` 返回（详情页按需拉取，前端已有该调用路径，只需把 `openDetail` 改为"列表数据无 content 时请求详情"）；
- 预计载荷 468 KB → **约 170 KB（−64%）**，gzip 后 < 50 KB；
- 顺手把 `summary` 也截到 160 字（当前 240 + 2000 双份，浪费 50 KB）。

### P1-2｜服务端单一实现 + 并发合并 + 生产兜底

**方案**：
1. 新建 `lib/api.mjs`：承载路由分发、`source` 过滤、字段裁剪、错误结构、404 文案；`server.js` 与 `src/index.js` 各剩约 20 行运行时适配（Express `res.json` / Worker `Response`）。
2. `lib/cache.mjs`：统一 TTL + **single-flight**（`inflightPromise`，同 key 的并发请求共享同一次抓取）+ `stale` 兜底（抓取失败且有过期缓存 → 返回旧数据 + `stale: true`）。
3. Worker 侧把缓存从 isolate 内存换成 **Cache API 或 KV**（至少让同一边缘节点共享），并给 `/api/news` 加 `Cache-Control: public, max-age=60, stale-while-revalidate=300`（替换 `no-store`）。
4. `refresh=1` 加最小间隔（如 60 s 内忽略强制刷新）。

**验证标准**：并发 20 个冷缓存请求 → 上游抓取轮次 = 1；某源全挂时 `/api/news` 仍返回旧数据且 `stale: true`。

### P1-3｜源可靠性加固

**方案**：① 每源 1 次重试（间隔 400~800 ms 抖动）② 连续失败 3 轮的源自动降频（从每轮抓改为每 3 轮抓一次）③ `errors` 升级为 `{source, message, lastOkAt, failStreak}` 并在 UI 提供"源健康"面板（背景设置面板旁边加一个入口，或顶栏点击展开）④ 超时按源分级（热榜 8 s、RSS 15 s）。

### P1-4｜让去重真正生效

**方案**：
1. **分开去重**：RSS 之间去重、热榜之间去重，**不跨 kind 合并**（避免热搜被新闻吞掉）。
2. 阈值分级：归一化标题完全相等 → 合并；相似度 ≥ 0.86 → 合并（当前 0.94 过严）；同时对"同域名 + 路径主体相同"的 URL 判重。
3. 相似度算法加固：2-gram Jaccard 之外，加"长度差异惩罚"（长度差 > 50% 直接不合并），减少误合并。
4. 合并时保留更强的来源（权重高者为主），`dupSources` 记录全部来源，`dupCount` 计入热度。

**验证标准**：真实抓取一轮后 `dupCount > 1` 的条目 > 0（当前 0）；人工抽查合并结果无明显误合并。

### P1-5｜热度权重归一化

**方案**：
1. 每项先归一到 0~1 再加权，权重和严格 = 1（消除 1.10 > 1 的截断）。
2. 让"来源权重"真正有区分度：把 `quality` 从"0.55 + w×0.42"改为按权重线性映射到较宽区间（如 `0.4 ~ 1.0`），或直接用 `w / maxW`。
3. 让"分类热度"有意义：`catFreq` 上限提到 0.15 量级，或改成"该分类条目数 / 全站条目数"的线性映射。
4. 让"多源重复"有效：`dup` 项与 P1-4 联动（`min(1, (dupCount-1)/2)`）。
5. **UI 语义**：把"热度 37%"改为**相对排名**（"全站第 12 热"）或热度档位（🔥🔥🔥），避免把 0~1 分数当百分比误导用户。
6. `heatScore` 保留在数据层（不进 UI 组件），符合原始约束。

**验证标准**：heatScore 不同取值数 > 条目数 × 0.7（当前 53/209）；来源权重不同的同热度条目排序有可观测差异；无条目触及上限 1.0。

### P1-6｜分类与源分布对齐

**方案**：
1. **补齐源**：体育（如 虎扑/懂球帝 走授权通道，或先接 新浪体育 RSS）、影视（触乐 + 豆瓣替代源）、国际（BBC中文/路透中文 RSS 等公开源）、AI（量子位恢复 + 机器之心等）。
2. **热榜独立成枝**：新增一个"热榜"主枝（或独立视觉层/独立颜色族），把 7 个平台话题从"国内"里移出，让"新闻"与"话题"在树上天然可分。
3. **分类单一来源**：`CATEGORIES` 由 API 返回（`/api/meta`）或前后端共用一份配置文件，删除 `TAG_ORDER` / `TAG_PRIORITY` 两份副本。
4. 主枝条目占比设软上限（如单枝 ≤ 40%），超限时布局上自动收窄扇区而不是让一枝占满。

### P1-7｜叶片宽度统一

**方案**：统一用 `textWidth()`（CJK 13.5 / ASCII 7.6）估算叶片宽度；由"每排容量 ÷ 平均标题宽"反推叶片宽度上限（而不是现在的 `88 + 字数×2.8`）；叶片固定高度下允许 1 行截断，但**截断点必须落在语义边界**（优先在 `：`、`，`、`｜` 处断句）；删除 `news-store.leafWidthFor` 这套死算法。

**验证标准**：叶片标题平均显示比例 ≥ 80%（当前 55%），且截断处不出现半截词语。

### P2 类｜按检查点 20 / 检查点 12 的清单逐项清理

- 死代码：删除 `slugify`、`fmtClock`、`el`、`clamp`、`leafWidthFor`、`leafMeta`、`findItem`、`getLeaf`、`heatPct`、`CATEGORIES` 死导出、`BOARD_TOP_PER_PLATFORM`（或反过来让 `views.js` 用它）。
- 死 CSS：删除 `.card*`（8 条）、`.lg-cards`、`.b-dup`、`.hc-dup`、`.leaf-meta`、被覆盖的 `.leaf-title{12.6px}`；把追加式堆叠整理为分区（视觉结果必须逐像素不变）。
- 字段：五组同义字段各留一个（建议留 `kind`、`category`、`source`、`publishedAt`、`rank`），其余作为别名在输出层一次性映射（不破坏现有前端）。
- 交互 8 项：`drag` 起点在指针数量变化时重置；`resize` 加防抖且**不重置用户视图**（只在首次或用户点"适应"时 fit）；触摸端点击叶片弹卡；`touch-action` 收窄到树容器并允许页面纵向滚动；移动端把操作提示做成可关闭的首次引导；`#zoom-fit`/`#zoom-reset` 合并（"适应"=重新入镜，"重置"=回到 1:1 并居中）；叶片加 `tabindex` + 方向键遍历 + 焦点环；悬停卡在缩放/滚动时重新定位。
- 错误处理：统一错误结构、加 Toast、把静默 `catch` 改为可见提示、加"源健康"面板。
- 仓库卫生：`.gitignore` 增补 `.wrangler/`、`.workbuddy-ai/`；把 `fix-verification.html`、旧报告、1.6 MB 图片、构建日志移出仓库（或归档到 `docs/`、`assets/`）。
- 部署：固定 `wrangler` 为 devDependency 并加 `deploy`/`preview` 脚本；加 GitHub Actions 跑 `npm run check`（test + lint）作为门禁；校正 `DEPLOY.md` 术语（明确"这是 Worker 项目"）；给未知路径返回 404 而不是软 200（SPA 只回落 `/detail/*`）。

---

## 四、D. 涉及的文件

| 优先级 | 文件 | 涉及内容 |
|---|---|---|
| P0 | `lib/news-core.mjs` | `stripHtml` 顺序（P0-1）、截断时机、`image` 抽取、去重（P1-4）、热度权重（P1-5）、列表字段裁剪（P1-1）、源重试（P1-3）、分类配置（P1-6） |
| P0 | `public/js/tree-view.js` | `layout()` 几何重构（P0-2）、`fit()` 语义缩放、`leafW` 宽度算法（P1-7）、拖动/缩放交互修复（P2-4） |
| P0 | `public/js/app.js` | 首屏缓存优先渲染、骨架屏、Toast、错误可见化、resize 防抖、交互按钮合并（P0-3 / P2-4 / P2-5） |
| P0 | `public/js/news-store.js` | localStorage 持久化、错误结构统一、`buildTreeModel` 精简、删除死函数 |
| P0 | `public/js/views.js` | 详情页按需取 `content`、热度语义化展示、悬停卡定位、`heatText` 去重 |
| P0 | `public/js/helpers.js` | 时间/文本宽度工具收敛、删除死函数 |
| P0 | `public/index.html` | 骨架屏结构、树容器 `touch-action`、可访问性属性（`aria-live`/`tabindex`） |
| P0 | `public/style.css` | 死规则清理、移动端布局、LOD 文字隐藏、真浅色主题、`prefers-reduced-motion` |
| P0 | `server.js` / `src/index.js` | 抽薄为运行时适配（P1-2）、stale 兜底、`refresh` 节流 |
| 新增 | `lib/api.mjs`、`lib/cache.mjs`、`lib/sources/*.mjs`、`test/*.test.mjs`、`.github/workflows/ci.yml` | 单一 API 实现、缓存与 single-flight、源适配器文件化、单测、CI |
| 配置 | `package.json`、`wrangler.jsonc`、`DEPLOY.md`、`.gitignore` | 固定 wrangler、`check`/`deploy` 脚本、部署文档校正、忽略项补全 |

---

## 五、E. 修改风险

| 改动 | 风险等级 | 风险点 | 缓解 |
|---|---|---|---|
| P0-4 先提交 | **极低** | 几乎无风险，只是把已有修复落库 | 提交信息写明"恢复线上正确性"，先本地验证再 push |
| P0-1 `stripHtml` 顺序 | **低** | 纯函数；可能让某些源的 `summary` 变短（因为不再把标签算进长度）；正则去标签对 `>` 出现在属性里的极端情况不完善 | 用 9 个真实 feed 样本做单测；只改顺序不改语义 |
| P1-1 载荷瘦身 | **低~中** | 前端详情页若拿不到 `content` 会退化为只显示摘要 → 必须同步改 `openDetail` 的取数逻辑 | 列表项保留 `summary`；详情页按需请求 `/api/news/:id`；两者同一批提交 |
| P1-5 热度权重 | **中** | 权重一变，热度排序与叶片宽度全变（视觉连锁）；用户可能"感觉排序变了" | 单独一次改动、附前后 Top20 对照表；保留 `heatScore` 字段名与 0~1 区间 |
| P1-4 去重增强 | **中** | 阈值放宽可能误合并不同新闻（比"漏合并"更伤） | 先离线跑一轮真实数据，人工抽查 20 组合并结果；阈值 0.86 起步，逐步下调 |
| P1-2 服务端抽层 | **中** | 涉及两个运行时，Express 5 与 Worker 的差异（`Request`/`Response` 语义、header 处理） | 抽层后必须**两个运行时都跑一遍**（`npm start` + `npx wrangler dev`）；保留旧实现直到新实现验证通过 |
| P1-3 源重试/降频 | **低~中** | 重试会拉长冷启动时间（当前已 30 s） | 必须与 P0-3（抓取与请求解耦）**同批或之后**做，不能先加重试 |
| P1-6 分类与源补齐 | **中** | 新源必然引入新的解析分支、新的失败模式；"热榜独立成枝"会改变树结构 | 先加源（`enabled` 可回退），再改分类；每加一源跑一次真实抓取 |
| **P0-2 布局重构** | **高** | 这是项目最核心的视觉资产，改错会破坏"必须是一棵二维树"的硬约束；且用户已习惯现有形态 | 单独分支开发；先做"新旧布局并排截图对照"再合并；硬约束清单逐条验收（有树干/主枝/分枝/叶片、无粒子、无随机、无散点）；`layout()` 保持纯函数便于单测 |
| P1-7 叶片宽度 | **中** | 宽度一变，树尺寸/包围盒/fit 缩放全变（与 P0-2 强耦合） | 与 P0-2 合并为同一次布局改动，避免二次返工 |
| P2 死代码/CSS 清理 | **低** | CSS 删除可能误删仍在生效的规则（选择器被动态拼接时 grep 不到） | 逐条确认引用后删；删完做一次"桌面 + 移动端"视觉对照 |
| P2-7 加 CI | **低** | CI 失败会阻断自动部署（这正是目的，但需先保证 check 通过） | 先让 `npm run check` 在本地全绿，再接入 workflow |
| P2-8 仓库卫生 | **低** | 移动/删除文件若被误提交可能影响历史 | 用 `git mv`；1.6 MB 图片移出仓库前确认无引用（已确认无） |

---

## 六、F. 推荐修改顺序

> 排序原则：**先让线上正确 → 再让数据干净 → 再让它跑得快 → 再让树真的像树 → 再让数据源稳 → 再补全分类 → 最后上锁（CI）→ 最后清理。**

```
第 0 步（10 分钟，最高优先）
  提交 P0-4：把已完成的 fakeTimeAgo 修复落库并推送
  理由：本地与线上不一致，任何后续改动都会与这轮修复混在一个 diff 里

第 1 批｜正确性（0.5 ~ 1 天）—— 都在数据层，风险低、可单测
  P0-1 stripHtml 解码顺序 + 截断时机 + image 抽取
  P1-5 热度权重归一化（附前后 Top20 对照）
  P1-7 叶片宽度统一（与 P0-2 同批亦可，但单独提交便于回滚）

第 2 批｜可用性（1 ~ 1.5 天）—— 让用户先"看得到"
  P1-1 接口瘦身（列表去 content）
  P0-3 首屏：抓取与请求解耦 + localStorage 缓存优先渲染 + 骨架屏 + 进度反馈

第 3 批｜树的本质（2 ~ 3 天，单独分支）—— 项目核心资产
  P0-2 布局改径向/扇区 + fit 语义缩放（LOD）
  验收：1440×900 叶片字号 ≥ 6px、宽高比 ≤ 1.8、无随机、无粒子

第 4 批｜可靠性（1 ~ 1.5 天）—— 在"抓取解耦"之后做
  P1-2 lib/api.mjs + lib/cache.mjs（TTL 统一 / single-flight / stale 兜底 / Cache-Control）
  P1-3 源重试 + 降频 + 源健康面板
  P1-4 去重增强（kind 分离 + 阈值分级）

第 5 批｜数据面（2 ~ 3 天）
  P1-6 补体育/影视/国际/AI 源 + 热榜独立成枝 + 分类配置单一来源

第 6 批｜工程防线（1 天）—— 上锁
  node:test 覆盖 5 个纯函数（calculateHeatScore / titleSimilarity / dedupe / parseFeedXml / stripHtml）
  固定 wrangler 版本 + npm run check + GitHub Actions 门禁
  校正 DEPLOY.md、补 .gitignore

第 7 批｜清理（0.5 天）
  P2-1 ~ P2-9：死代码、死 CSS、字段归一、交互 8 项、仓库卫生、可访问性、真浅色主题
```

**不建议的顺序**（会返工）：不要先做第 3 批（布局）再做第 1 批 —— 因为 P1-7（叶片宽度）会改变树的尺寸，布局白做一次；不要先加源重试（P1-3）再加首屏解耦（P0-3）—— 重试会让 30 s 冷启动变成 60 s。

---

## 七、G. 哪些地方必须保留（不可动）

1. **二维真实树形结构本身**：树干 → 主枝（分类）→ 分枝（来源）→ 新闻叶片。任何布局改动都必须让这四个层级仍然可辨识。
2. **确定性布局、无随机**：`layout()` 由数据唯一决定，同数据必得同树。**禁止**粒子网络、随机散点、漂浮卡片、纯列表替代、Mock 数据。
3. **真实数据纪律**：不做 Mock；`PLANNED_SOURCES` 这种"诚实登记不可达源"的做法要继续保持。
4. **不伪造时间戳**（`hasRealTime` 机制）：这是上一轮最关键的正确性修复，必须在任何重构中守住。
5. **单一数据引擎 + 源注册表**：`lib/news-core.mjs` 被本地与生产共用；源用配置数组描述（`enabled` / `weight` / `category` / `kind`），可开关可加权。
6. **`heatScore` 是数据层的纯函数**，不写进 UI 组件。
7. **三视图共用一份数据与同一套跳转逻辑**（树/列表/热榜 → 同一个详情页），不新建第二套详情系统。
8. **API 契约**：`/api/news`、`/api/news/:id`、`/api/health` 的路径与语义不变（前端与外部都依赖）。
9. **逐源 try/catch**：单源失败不影响整体（`collectAll` 这一点做对了）。
10. **零依赖零构建 + Workers Static Assets 部署形态**：这是本项目相对参照项目最大的运维优势，不要引入打包器/框架。
11. **四层分离**：数据（news-store）/ 几何（layout）/ 渲染（build）/ 交互（attach）边界清晰，重构时保持。
12. **已验证的交互正确性**：拖拽与点击的 `_suppressClick` 判别、指针捕获、以指针为锚点的缩放数学、悬停时高亮所属主枝/分枝/叶柄 —— 这些是"做对了的部分"，别改坏。
13. **空数据三处兜底文案与详情错误页**：已经存在且正确。
14. **无密钥入库**：继续不写任何 Token/Secret。

---

## 八、H. 哪些地方可以重构

| 可重构对象 | 现状 | 重构方向 |
|---|---|---|
| **树布局几何** | 主枝沿 X 线性平铺 → 宽高比 6.7:1、fit 后 2px 字 | 径向/扇区布局（或纵向分层 + 中轴树干），目标宽高比 1.0~1.8 |
| **fit 策略** | 整树硬缩到入镜 | 语义缩放（LOD）+ 可读缩放下限 + 聚焦视口 |
| **叶片宽度** | 三套算法并存（1 套生效 2 套死） | 统一到 `textWidth()`，按排容量反推，语义边界断句 |
| **服务端** | 两份几乎重复的实现，能力不对等 | `lib/api.mjs` 单一实现 + 两个 ~20 行运行时适配 |
| **缓存** | 两套 TTL、isolate 内存、无 single-flight、`no-store` | `lib/cache.mjs`：统一 TTL + 源级 interval + single-flight + stale 兜底 + Cache API/KV |
| **解析层** | 正则解析 XML（对 CDATA/命名空间/GBK 有已知局限）；`stripHtml` 顺序错误 | 保留正则但**标注已知局限** + 真实样本单测；修 `stripHtml` 顺序；`content` 截断移到清洗后 |
| **热度算法** | 权重和 1.10、28% 权重只有 0.02 区分度、dup 项恒 0 | 各项先归一化再加权（和=1）；来源权重线性展开；dup 与去重联动；UI 改语义化展示 |
| **去重** | 跨 kind 全局、阈值 0.94 从不命中 | kind 分离 + 阈值分级 + URL 判重 + 长度惩罚 |
| **源解析结构** | `parseHot()` 90 行 if/else 链，源与解析同文件 | 每源一个适配器文件（`lib/sources/weibo.mjs` …）+ 注册表；影响半径收敛到单文件 |
| **字段模型** | 五组同义字段双写 + `heatPct`/`dupSources` 死字段 + 缺 `tags`/`image` | 每语义保留一个字段，输出层做一次别名映射（不破坏前端）；补 `image` 抽取 |
| **分类配置** | 三处不一致（CATEGORIES / TAG_ORDER / TAG_PRIORITY），`CATEGORIES` 死导出 | 单一来源（API 返回或共用配置） |
| **前端数据层** | 纯内存、错误结构不统一、10 余处静默 catch | localStorage 持久化 + 统一错误结构 + Toast |
| **交互层** | 8 项缺陷（缩放后拖动跳变、resize 重置、触摸 hover、touch-action、隐藏提示、重复按钮、无键盘、卡片不跟随） | 逐项修；`touch-action` 收窄；移动端首次引导；叶片键盘可达 |
| **CSS** | 634 行追加式堆叠 + 15 处死规则 | 分区整理（视觉不变）+ 删死规则 + 补 `prefers-reduced-motion` + 真浅色主题 |
| **详情页** | 无 content 时只显示摘要、无上下条、深链会失效 | 按需取 content + 上一条/下一条 + 可选持久化（KV/D1）让深链长期有效 |
| **部署链** | 无 CI、wrangler 未固定、文档术语矛盾、软 404 | 固定 wrangler + `npm run check` + Actions 门禁 + 文档校正 + SPA 只回落 `/detail/*` |

---

## 九、新闻树整改路线图（按实际开发顺序）

### 阶段 0 · 止血（当天完成）
- **目标**：本地与线上一致，线上恢复正确性。
- **动作**：提交并推送已完成的 `fakeTimeAgo` 修复（3 个文件）。
- **验收**：`git status` 干净；线上 `/api/health` 的 `itemCount > 0`；抽 3 条热榜条目确认 `publishedAt === null` 且 `hasRealTime === false`。
- **不做**：不夹带任何新功能。

### 阶段 1 · 数据正确性（1 天）
- **目标**：详情页与悬停卡不再出现 HTML 源码；热度真正能区分；叶片文字宽度可信。
- **动作**：`stripHtml` 顺序修复 + 截断后置 + `image` 抽取；热度权重归一化；叶片宽度统一到 `textWidth`。
- **验收（可量化）**：
  - 209 条中 `summary`/`content` 含 `<标签>` 的条目 = **0**
  - `content` 不以未闭合标签结尾 = **0 条**
  - `heatScore` 不同取值数 ≥ 条目数 × 0.7
  - 叶片标题平均显示比例 ≥ **80%**（当前 55%）
  - `image` 非空条目 > 0

### 阶段 2 · 首屏与载荷（1.5 天）
- **目标**：用户 1.5 s 内看到内容，而不是 30 s 空白。
- **动作**：列表接口去 `content`；抓取与请求解耦（预热 + 202 warming）；localStorage 缓存优先渲染；骨架屏 + 进度反馈；`refresh` 节流。
- **验收**：冷启动首屏骨架 < 1.5 s；有缓存时可见内容 < 300 ms；`/api/news` 载荷 < 200 KB；同步阻塞响应 < 5 s；并发 20 请求只触发 1 轮抓取。

### 阶段 3 · 树的可读性（2~3 天，核心阶段）
- **目标**：**第一眼就是一棵能读的树。**
- **动作**：布局改径向/扇区（或中轴纵向分层）；`fit` 加可读缩放下限与 LOD（<0.42 隐文字、<0.22 按来源聚类）；宽高比压到 1.0~1.8。
- **验收**：1440×900 默认视图叶片字号 ≥ **6 px**；单分类视图 ≥ 9 px；宽高比 ≤ 1.8；两次 `layout()` 结果完全一致（无随机）；硬约束逐条通过（有树干/主枝/分枝/叶片，无粒子网络、无散点、无漂浮卡片、非纯列表）。
- **方法**：单独分支 + 新旧并排截图对照，确认后再合并。

### 阶段 4 · 服务端与源可靠性（1.5 天）
- **目标**：一个源死了不影响用户，缓存不再放大上游压力。
- **动作**：`lib/api.mjs` + `lib/cache.mjs`（TTL 统一、single-flight、stale 兜底、Cache API/KV、`Cache-Control`）；源重试 + 失败降频 + 源健康面板；去重 kind 分离与阈值分级。
- **验收**：并发 20 冷请求 → 上游抓取 1 轮；拔掉任意 2 个源仍返回旧数据且 `stale: true`；`dupCount > 1` 条目 > 0；UI 能列出失败源名与原因。

### 阶段 5 · 分类与源生态（2~3 天）
- **目标**：主枝 ≥ 5 个、分布均衡、"新闻"与"热榜"在树上有区分。
- **动作**：补体育/影视/国际/AI 源；热榜独立成枝；分类配置单一来源。
- **验收**：主枝 ≥ 5 个；单枝条目占比 ≤ 40%；体育与影视主枝可见；热榜主枝与新闻主枝颜色/位置可区分。

### 阶段 6 · 工程防线（1 天）
- **目标**：改错代码上不了线。
- **动作**：`node:test` 覆盖 5 个纯函数（含真实 feed 样本）；固定 `wrangler` 版本 + `npm run check`；GitHub Actions 门禁；校正 `DEPLOY.md`；SPA 只回落 `/detail/*`。
- **验收**：`npm run check` 全绿；故意提交一个错误断言 → CI 失败并阻断部署；`npx wrangler dev` 与 `npm start` 行为一致。

### 阶段 7 · 清理与打磨（0.5~1 天）
- **目标**：仓库干净、体验细节补齐。
- **动作**：死代码/死 CSS/字段归一；交互 8 项修复；仓库卫生（`.gitignore`、临时文件归档）；可访问性（键盘可达、`aria-live`、`prefers-reduced-motion`）；真浅色主题。
- **验收**：全项目无 0 引用符号；`.card*`/`.lg-cards` 等死规则删除后视觉无变化；树叶片可 Tab 到达并用方向键遍历；移动端可正常滚动页面。

---

## 十、一句话结论

**这个项目的产品创意（二维真实新闻树）与工程取舍（零依赖零构建、单一数据引擎、不伪造数据）都是真优势，值得保留；但当前它处在"结构对、体验不可用、数据有硬伤"的状态** —— 最致命的三件事是：**详情页在给用户看 HTML 源码（P0-1）、树默认视图字号只有 2px（P0-2）、冷启动 30 秒空白（P0-3）**，而最紧急的一件事是**把已修好的改动先提交上线（P0-4）**。整改顺序必须是「先正确 → 再快 → 再像树 → 再稳 → 再全 → 后上锁」，其中**布局重构（阶段 3）是唯一需要单独分支、单独验收的高风险改动**，其余都是低风险增量。

---

*本报告未修改任何项目代码；所有实测数据可用 `collectAll()` 探针与 `layout()` 复算脚本复现。*
